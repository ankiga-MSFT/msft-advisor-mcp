{
  "rules": [
    {
      "ruleId": "Support_for_TLS_10_and",
      "description": "Support for TLS 1.0 and TLS 1.1 ends on the retirement date.",
      "query": "where tostring(properties['minimumTlsVersionAllowed']) in ('1.1','1.0')",
      "fix": "Review and remediate based on recommendation"
    }
  ]
}
