{
  "rules": [
    {
      "ruleId": "Create_up_to_two_maintenance",
      "description": "Create up to two maintenance windows for the Azure Virtual Desktop agent, side-by-side stack, and Geneva Monitoring agent to get updated so that updates don't happen during peak business hours.",
      "query": "where isnull(properties.agentUpdate)",
      "fix": "Create up to two maintenance windows for the Azure Virtual Desktop agent, side-by-side stack, and Geneva Monitoring agent to get updated so that updates don't happen during peak business hours."
    }
  ]
}
