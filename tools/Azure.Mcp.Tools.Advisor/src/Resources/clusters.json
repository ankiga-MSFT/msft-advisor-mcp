{
  "rules": [
    {
      "ruleId": "Customers_running_HDInsight_50_no",
      "description": "Customers running HDInsight 5.0 no longer receive support.",
      "query": "where (strcat(split(properties.clusterVersion,'.')[0],'.',split(properties.clusterVersion,'.')[1])) in ('5.0')",
      "fix": "Review and remediate based on recommendation"
    },
    {
      "ruleId": "Customers_running_HDInsight_40_no",
      "description": "Customers running HDInsight 4.0 no longer receive support and no new clusters can be created after the retirement date.",
      "query": "where (strcat(split(properties.clusterVersion,'.')[0],'.',split(properties.clusterVersion,'.')[1])) in ('4.0')",
      "fix": "Review and remediate based on recommendation"
    }
  ]
}
