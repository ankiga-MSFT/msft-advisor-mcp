{
  "rules": [
    {
      "ruleId": "Use_an_explicit_connectivity_method",
      "description": "Use an explicit connectivity method such as NAT gateway or a Public IP. After March 31, 2026, new virtual networks will default to creation of private subnets, which are intentionally designed to block default outbound access connectivity.",
      "query": "where computeresources |  mv-expand defaultOutboundConnectivityEnabled = properties.defaultOutboundConnectivityEnabled | where defaultOutboundConnectivityEnabled == 'true'",
      "fix": "Use an explicit connectivity method such as NAT gateway or a Public IP"
    }
  ]
}
