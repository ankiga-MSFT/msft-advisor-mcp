{
  "rules": [
    {
      "ruleId": "The_Azure_Image_Builder_service",
      "description": "The Azure Image Builder service lacks availability zones support. Replicating Image Templates to a secondary region will enable the build of new images in secondary region.",
      "query": "mv-expand distribution=properties.distribute | where array_length(parse_json(distribution).replicationRegions) == 1",
      "fix": "The Azure Image Builder service lacks availability zones support. Replicating Image Templates to a secondary region will enable the build of new images in secondary region."
    }
  ]
}
