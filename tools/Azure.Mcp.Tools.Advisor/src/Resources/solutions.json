{
  "rules": [
    {
      "ruleId": "To_monitor_connections_between_servers",
      "description": "To monitor connections between servers, processes, inbound and outbound connection latency, and ports across any TCP-connected architecture, you need to migrate to Azure Monitor VM insights.",
      "query": "where plan.product == 'OMSGallery/ServiceMap'",
      "fix": "To monitor connections between servers, processes, inbound and outbound connection latency, and ports across any TCP-connected architecture, you need to migrate to Azure Monitor VM insights."
    }
  ]
}
