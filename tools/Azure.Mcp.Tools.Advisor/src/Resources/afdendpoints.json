{
  "rules": [
    {
      "ruleId": "We_detected_your_subscription_contains",
      "description": "We detected your subscription contains multiple Front Door Standard/Premium profiles with a small number of endpoints on them. You can save costs in base fees by using multiple endpoints within one profile. You can use a maximum of 10 endpoints with Standard tier and 25 endpoints with Premium tier.",
      "query": "extend frontDoorId = substring(id, 0, indexof(id, '/afdendpoints/')) | summarize numberEndpoints = count() by frontDoorId, subscriptionId | where numberEndpoints < 5 | summarize numberProfiles = count() by subscriptionId | where numberProfiles > 1",
      "fix": "use a maximum of 10 endpoints with Standard tier and 25 endpoints with Premium tier"
    }
  ]
}
