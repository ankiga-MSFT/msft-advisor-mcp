{
  "rules": [
    {
      "ruleId": "To_avoid_service_disruptions_migrate",
      "description": "To avoid service disruptions, migrate workloads to Azure Managed Redis before the retirement date.",
      "query": "where kind =~ 'v1'",
      "fix": "To avoid service disruptions, migrate workloads to Azure Managed Redis before the retirement date."
    }
  ]
}
