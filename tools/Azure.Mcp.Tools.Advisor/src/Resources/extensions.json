{
  "rules": [
    {
      "ruleId": "Migrate_Desired_State_Configuration_Extension",
      "description": "Migrate Desired State Configuration Extension for Azure Virtual Machines to Azure Machine Configuration in Azure Resource Manager. Desired State Configuration Extension for Azure Virtual Machines is retiring.",
      "query": "where tolower(name) == 'microsoft.powershell.dsc' and toupper(substring(id, 0, indexof(id, '/extensions'))) != ''",
      "fix": "Migrate Desired State Configuration Extension for Azure Virtual Machines to Azure Machine Configuration in Azure Resource Manager. Desired State Configuration Extension for Azure Virtual Machines is retiring."
    }
  ]
}
